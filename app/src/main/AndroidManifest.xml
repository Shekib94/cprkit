<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <!-- سشن محلی: سرور روی گوشی GM و اتصال بازیکن‌ها در شبکه‌ی محلی -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!--
      دسترسی به شبکه‌ی محلی (اندروید ۱۷ / API 37+).
      سشن LAN روی HTTP ساده در همان شبکه کار می‌کند. تا API 36 این دسترسی از راه
      INTERNET به‌صورت ضمنی می‌آمد، ولی از targetSdk 37 به بعد شبکه‌ی محلی
      «به‌صورت پیش‌فرض بسته» است و باید همین مجوز زمان‌اجرا (گروه NEARBY_DEVICES)
      از کاربر گرفته شود؛ وگرنه ساخت سشن و پیوستن بازیکن‌ها بی‌صدا شکست می‌خورد.
      درخواستش در ui/components/LocalNetworkPermission.kt انجام می‌شود.
      نکته: برای targetSdk کمتر از 37 نباید این مجوز را declare/request کرد.
    -->
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />
    <!-- اسکن QR برای پیوستن به سشن -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.CyberpunkGMTool">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CyberpunkGMTool"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>